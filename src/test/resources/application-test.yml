spring:

  application:
    name: document-render-service

  service:
    path:
      prefix: /document-render-java

  internal:
    asset-registry:
      url: ${TEMPLATE_REGISTRY_ADDR:https://api.cidev.aws.chdev.org}

server:
  port: 9191
